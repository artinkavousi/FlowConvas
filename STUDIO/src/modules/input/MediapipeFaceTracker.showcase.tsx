import React from 'react';
import { MediapipeFaceTrackerVisualizer } from './MediapipeFaceTracker';

export default function MediapipeFaceTrackerShowcase() {
  return (
    <div className="h-full w-full bg-black flex items-center justify-center">
      <MediapipeFaceTrackerVisualizer />
    </div>
  );
}
